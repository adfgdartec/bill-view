import Header from "../components/Header/Header";

export default function SignUp() {
    return (
        <div>
            <Header userAuth={false} />
            <p>You are in the SignUp page.</p>
        </div>
    );
}